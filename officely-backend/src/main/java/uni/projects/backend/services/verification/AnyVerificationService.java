package uni.projects.backend.services.verification;

import uni.projects.backend.models.user.User;

public class AnyVerificationService implements VerificationService {

    @Override
    public void verifyUser(User user) {
        return;
    }
}
